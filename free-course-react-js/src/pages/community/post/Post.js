import React, { useEffect, useRef, useState } from "react";
import {
  AppBar,
  Dialog,
  Divider,
  IconButton,
  List,
  ListItem,
  ListItemText,
  Paper,
  Toolbar,
  Typography,
  Box,
  useTheme,
} from "@mui/material";
import QuillEditor from "components/rich-text-editor/QuillEditor";
import { Editor } from "@tinymce/tinymce-react";

const Post = (props) => {
  return <Box>show post</Box>;
};

export default Post;
